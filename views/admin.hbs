<!DOCTYPE html>
<html lang="ru">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Админка</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">
    <link rel="stylesheet" href="index.css">
</head>

<body>
    <header>
        <h1>Кошкин дом <span class="badge text-bg-danger">Админка</span></h1>
    </header>

    <form action="/admin-add" method="POST">
        <div class="input-group mb-3">
            
            <span class="input-group-text">Кличка</span>
            <input name="name" type="text" class="form-control">
            
            <span class="input-group-text">Возраст</span>
            <input name="age" type="number" class="form-control">

            <span class="input-group-text">Порода</span>
            <input name="breed" type="text" class="form-control">

            <select name="sex" class="form-select">
                <option value="male">Мальчик</option>
                <option value="female">Девочка</option>
            </select>


            <button type="submit" class="btn btn-outline-secondary">Добавить</button>
        </div>
    </form>

    <table class="table">
        <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">Фото</th>
                <th scope="col">Имя</th>
                <th scope="col">Возраст</th>
                <th scope="col">Пол</th>
                <th scope="col">Порода</th>
                <th scope="col">Уже дома?</th>
            </tr>
        </thead>
        <tbody>
            {{#each items}}
            <tr>
                <th scope="row">{{@index}}</th>
                <td><img src="{{this.image}}" width="40"></td>
                <td>{{this.name}}</td>
                 <td>{{this.age}}</td>
                <td>
                    <span class="badge {{this.sex}}">
                        {{this.sex}}
                    </span>
                </td>
               
                <td>{{this.breed}}</td>

                <td>
                    {{#if this.isWanted}}
                         <a href="/admin-home?id={{this.id}}" class="btn btn-primary">Хотят забрать</a>
                    {{else}}
                        {{#if this.isActive}}
                            В поиске дома
                        {{else}}
                            Уже дома
                        {{/if}}
                    {{/if}}

                    
                </td>
            </tr>
            {{/each}}
        </tbody>
    </table>
    </div>
</body>

</html>